
This directory contains tools used to update and deploy the package for use at the telescope.  All of these scripts were almost entirely written by Claude.

- `deploy.sh`: the main script used to update and deplay new versions of the code
- `write_version.py`: maintains the automated versioning of the code.
- `reflow_docstrings.py`: A development script used to reformat docstrings.

